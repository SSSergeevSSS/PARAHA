<Page x:Class="Administration.Pages.Administration"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Administration.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Administration">

    <Grid>
        <DataGrid Name="DGrid"  AutoGenerateColumns ="false" IsReadOnly ="true">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding ID}"/>
                <DataGridTextColumn Header="Название" Binding="{Binding Title}"/>
                <DataGridTextColumn Header="Адрес" Binding="{Binding Address}"/>
                <DataGridTextColumn Header="Почта" Binding="{Binding Mail}"/>
                <DataGridTextColumn Header="ID_жителя" Binding="{Binding IDPopulation}"/>
                <DataGridTextColumn Header="ID_заявки" Binding="{Binding IDApplication}"/>
                <DataGridTextColumn Header="ID_сотрудника" Binding="{Binding IDStaff}"/>
            </DataGrid.Columns>
        </DataGrid>
        <Button Height="40"  Width="150" HorizontalAlignment="Right" VerticalAlignment="Bottom" Content="Назад" 
                FontSize="20" Click="BtnBack_Click" Margin="0,0,628,23"></Button>


    </Grid>
</Page>
